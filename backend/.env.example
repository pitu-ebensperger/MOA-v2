# Server
PORT=4000
NODE_ENV=development

# Usuarios (Seeds)
ADMIN_PASSWORD=admin123
CLIENTS_PASSWORD=Cliente123!

# Database PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=
DB_NAME=moa
DB_POOL_MAX=20
DB_POOL_IDLE_TIMEOUT_MS=30000
DB_POOL_CONNECTION_TIMEOUT_MS=2000

# JWT
JWT_SECRET=change-this-in-production-use-strong-random-string
JWT_EXPIRES_IN=24h
JWT_ADMIN_EXPIRES_IN=1d

# Email SMTP
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-specific-password
SMTP_FROM=noreply@moa.cl
FRONTEND_URL=http://localhost:5174

# Password Reset
PASSWORD_RESET_CLEANUP_ENABLED=true
PASSWORD_RESET_CLEANUP_INTERVAL_MINUTES=60
PASSWORD_RESET_CLEANUP_LOGS=false

# CORS
CORS_ORIGINS=http://localhost:5174

# Dashboard (opcional)
# ANALYTICS_REFERENCE_DATE=2024-11-30

